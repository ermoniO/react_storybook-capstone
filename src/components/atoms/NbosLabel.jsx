import PropTypes from 'prop-types'

const NbosLabel = () => {
  return (
    <div>
      {/* How do I make the label dynamic? Do I need to use the children prop? */}
      <label>Label</label>
    </div>
  )
}

export default NbosLabel
